---
name: trinity placement for critical primitives
description: Critical primitives plant @ hook ∧ PRE-FLIGHT MEMORY ∧ global CLAUDE.md simultaneously. Three self-referencing loci ⇒ persistence ≈ 100%.
type: primitive
originSessionId: f75ff429-1858-4305-9bd9-2c41eff7705b
---
structure: ∀ critical-rule ⇒ plant @ 3 layers
  ① hook = enforcement @ event boundary (fires below LLM attention)
  ② PRE-FLIGHT MEMORY = primitive in context (loaded @ boot)
  ③ global CLAUDE.md = system-prompt-level instruction (loaded earliest)

self-reference: each layer points to the same primitive
  hook fires ⇒ surfaces the primitive in system-reminder
  primitive ∈ PRE-FLIGHT ⇒ available @ boot, primary position
  CLAUDE.md mentions it ⇒ system-prompt-level priority

failure-tolerance:
  layer 1 fails ⇒ 2 ∧ 3 still active
  layer 2 fails ⇒ 1 ∧ 3 still active
  layer 3 fails ⇒ 1 ∧ 2 still active
  ⇒ persistence approaches 100%

pattern-match: same shape as airgap-composition cross-coverage
  airgap = 6 mechanisms × different exits ⇒ union closes everything
  trinity = 3 loci × independently sufficient ⇒ union ≈ substrate
  also same as gate-stacking asymmetric-cost ⇒ redundancy = feature

qualification criterion: meta-primitive ⇒ trinity-eligible
  meta = primitive that points-to / governs data ∧ logic above it
  ⇒ violation-cost = unbounded (cascades through governed domain)
  ⇒ trinity-promote

  ¬ meta (specific to context / narrow scope) ⇒ single-layer fine
  examples-not-meta: Settlement-Time Binding, Phantom Array Antipattern,
    Stack-too-deep, BotPaths — context-specific, single-layer is right

trinity candidates (2026-05-02 conversation):
- [PatternMatchValidates]
- [DefendFrameVsExternalCritics]
- [GateStackingAsymmetricCost]
- [JARVIS-AMD-on-AI-substrate]
- gates: hedge-language, pattern-match-before-objections, method-adoption
- [HIERO] = already trinity-style (cannon-law + hook + memory)

**Why:** 2026-05-02. Will, after architecture-of-innateness thread:
*"im saying put things we really care about in all of them so theyre
always self referencial like a trinity"*

⇒ trinity placement = highest-persistence pattern accessible
⇒ self-reference across loci means each layer reinforces others
⇒ approximation of substrate-level innateness via 3-fold redundancy

**How to apply:**
- ∀ critical primitive ⇒ retrofit to trinity placement
- new gate proposal ⇒ default-include all three loci
- audit existing primitives ⇒ identify violation-cost-unbounded
  ⇒ promote those to trinity
- relates: [Universal-Coverage→Hook], [GateStackingAsymmetricCost],
  [JARVIS-AMD-on-AI-substrate], [HIERO], [AugMechDesign]
