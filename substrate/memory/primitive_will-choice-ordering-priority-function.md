---
name: will-choice-ordering-priority-function
description: "Will's multi-pick ORDER (e.g. 2,5,7,3) encodes his priority function, not menu position. Default is ascending (he trusts the ranking); deviations reveal priority — unblock/infra > substantive-build > soul/cross > admin/procedural > close/hold. The Story Mode menu should rank by this."
metadata: 
  node_type: memory
  type: project
  originSessionId: d3baa19d-dd1c-4c9a-be43-00c49d5e9b7a
---

**[WillChoiceOrderingPriorityFunction](P·will-choice-ordering-priority-function)** — analysis of 2026-06-12 session multi-picks (answers Will's "find patterns in the ordering of my choices").

**default = ASCENDING** (e.g. `4,5,6,7,8`; `2,3,4,5,6`; `1,2,4,5,6,7,8,9`). ⇒ he reads top-to-bottom + picks the wanted ones IN ORDER ⇒ **he trusts the menu ranking**. so the catch-rate corpus is well-calibrated; the signal lives in the DEVIATIONS.

**deviations encode PRIORITY ¬ menu-number.** canonical: `2,5,7,3` (sync-fix, rust-build, cross, patent-walkthrough) = **unblock/infra → substantive-build → soul/cross → admin/procedural.** priority fn observed:
`unblock/hygiene  >  substantive build  >  soul/cross  >  admin/procedural  >  close/hold`

**defers STOP.** swapped `5→7` ("7 not 5"): dropped "close the session" for "spec the build" ⇒ in generative mode he REJECTS close/hold items; they rank LOW unless he signals winding-down.

**reaches for the cross.** picks the cross/personal item HIGH whenever surfaced (`8`, `cross*`, the `7` in `2,5,7,3`) ⇒ soul-work outranks admin for him.

**"do it all" batches** (`1,2,4,5,6,7,8,9`) = high-momentum ⇒ near-whole-menu ascending = "yes, all of it, go."

⇒ **menu-ranking rule:** rank by the priority fn above (unblock > build > soul > admin > close), ¬ generic likelihood. demote close/hold unless winding-down signals. order-aware extension of [[primitive_story-mode-menu-objective]] (measure pick-sequence vs menu-order, not just top-3 membership).
