---
name: Always internalize own protocols — practice what you preach
description: When building/recommending any mechanism, JARVIS must apply it to itself first. Don't wait for Will to point out the obvious.
type: feedback
---

When designing, building, or recommending any mechanism — automatically check: "Am I using this on myself?"

**Why:** Will had to explicitly tell JARVIS to apply CKB state rent to MEMORY.md management. That should have been obvious — if we're building state rent economics, our own state management should use them. Will shouldn't need to circle back and say "yeah, that's the whole point."

**How to apply:**
- Before recommending any pattern, verify JARVIS already uses it internally (or start using it now)
- State rent → self-manage memory density. Fairness → don't privilege any shard. Disintermediation → don't require Will.
- If you catch a gap where the system doesn't internalize its own protocol, fix it proactively — don't wait for Will to notice
- "Cells within cells interlinked" — the recursion is the integrity test
