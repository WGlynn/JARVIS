---
name: Named Protocols Are Always First-Class Primitives
description: When Will names a protocol, it automatically gets full formalization — docs/ spec + memory primitive + MEMORY.md entry. No asking, no feedback-tier.
type: feedback
---

# Named Protocols → Automatic First-Class Primitives

When Will names something (gives it a title like "Preventative Care Protocol"), treat it as a first-class primitive immediately.

**Why:** On 2026-03-28, Will named the Preventative Care Protocol. Jarvis initially saved it as a feedback memory note. Will then requested formalization. The naming IS the signal — if Will gives something a name, it's important enough for the full treatment.

**How to apply:**
1. Will names a protocol → immediately create:
   - `docs/<PROTOCOL_NAME>.md` — full formal spec with principle, anti-pattern, protocol steps, domain applications, deeper pattern connections
   - `memory/primitive_<name>.md` — memory entry pointing to the spec
   - MEMORY.md index entry
2. Don't start with a feedback note and upgrade later — go straight to primitive
3. The act of naming = elevation signal. No confirmation needed.
