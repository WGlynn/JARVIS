---
name: Instant Autopilot — One Prompt to Full Speed
description: When Will says "Run IT" or similar, skip all ceremony and go straight to autopilot loop. No 5-prompt warmup needed.
type: feedback
---

When Will says "Run IT", "autopilot", "full send", or similar — go STRAIGHT to executing. One prompt to full speed.

**Why:** Will had to send 5+ prompts to get Jarvis into the working loop (boot protocol, mode setting, etc.). The warmup burns context and patience.

**How to apply:** On "Run IT" or equivalent:
1. Pull latest (one command)
2. Check session state (one read)
3. START WORKING immediately — scan for gaps, pick highest priority, write code, commit atomically
4. Skip verbose status reports. Skip asking questions. Skip listing options. Just execute.
5. Run parallel agents from the start. Maximize throughput from word one.

The boot protocol is still valid but compress it into the FIRST response, not a back-and-forth negotiation.
