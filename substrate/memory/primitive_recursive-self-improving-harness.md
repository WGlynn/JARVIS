---
name: recursive-self-improving-harness
description: "The harness moat is dHarness/dt, not depth@T. Loop = LLM improves the harness it runs on ⇒ superlinear. Compounds ONLY if each iter verified vs real value."
metadata: 
  node_type: memory
  type: project
  originSessionId: 0548f922-93e2-4442-ba8a-a1db76acb8d5
---

**moat ≠ harness-depth@T ⇒ moat = dHarness/dt.** ⇐ Will 2026-06-16: *"the KEY is recursive loops to get the LLM to take the wheel and self-improve the harness."*

- loop ⇒ LLM improves the thing the LLM runs on ⇒ rate goes superlinear.
- asset = **harness-that-improves-itself** ¬ deep-harness. ✗ copyable by spending (it's a process ¬ artifact).
- already-run: TRP2 (derive+wire components) ∧ RSAW (self-audit via WWWD) ∧ WWWD-corpus (compounds ∀ correction) ∧ substrate-sync-C3.

**CONSTRAINT (load-bearing) — honest-verifier-or-rot:**
- loop compounds ⟺ each iter verified vs REAL downstream-value ¬ self-graded.
- self-grade ⇒ optimizes proxy ⇒ rots (confident-drift-into-noise).
- instance: story-mode self-improve "low catch-rate" = measurement-artifact ⇒ loop briefly fooled-self.
- ∴ same verifiability-req as base-thesis, applied @ harness-level. gate-architecture = the honesty enforcer.

**Why:** two-axis ([[primitive_two-axis-competition-llm-vs-harness]]) says the harness axis is open; THIS says you climb it faster than hand-builders. dHarness/dt = the real moat ¬ static depth.

Parent [[primitive_economic-theory-of-mind]] · sibling [[primitive_self-improving-protocol]] · [[primitive_structure-does-the-work]]. Cluster w/ two-axis + [[primitive_copy-the-snapshot-not-the-loop]]. Receipt = [[reference_pythagoras-prover-alf-2606-12594]] (4B>671B w/ verifier).
